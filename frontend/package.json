{
  "name": "copilot-proxy-frontend",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf ../internal/web/dist && mkdir -p ../internal/web/dist && cp src/index.html src/styles.css ../internal/web/dist/ && cp ../copilot_proxy.png ../internal/web/dist/favicon.png && tsc -p tsconfig.json",
    "build:all": "npm run build && cd .. && go build -o bin/copilot-proxy ./cmd/copilot-proxy"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
