{
  "name": "copilot-proxy-frontend",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js && tsc -p tsconfig.json",
    "build:all": "node build.js && tsc -p tsconfig.json && cd .. && go build -o bin/copilot-proxy ./cmd/copilot-proxy"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
