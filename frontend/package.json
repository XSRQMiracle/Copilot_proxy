{
  "name": "copilot-proxy-frontend",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf ../internal/web/dist && mkdir -p ../internal/web/dist && cp src/index.html src/styles.css ../internal/web/dist/ && tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
